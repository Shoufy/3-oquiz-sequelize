<%- include('partials/header') %>

        <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
            <h2 class="h1 display-4"><%= locals.userConnected.firstname %> <%= locals.userConnected.lastname %></h2>
          </div>

          <ul style="text-align: left">
              <% for (let key in locals.userConnected) { %> 
            <li> <%= key %> : <%= locals.userConnected[key]%> </li>
            <% } %> 
          </ul>


        <%- include('partials/footer') %>